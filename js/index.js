$(document).ready(function () {
	$(".movie-card-area-slick").slick({
		slidesToShow: 4,
		dots: false,
		centerMode: false,
	});

	$(".horizontal-scroll-area-slick").slick({
		slidesToShow: 3,
		dots: false,
		centerMode: false,
	});
});

// Slick version 1.5.8
